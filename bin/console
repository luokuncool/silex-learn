#!/usr/bin/env php
<?php

use Silex\Application;
use SilexLearn\Command\DemoCommand;

set_time_limit(0);
/** @var Application $app */
$app = require_once __DIR__ . '/../bootstrap.php';

$console = $app['console'];
$console->add(new DemoCommand());
$console->run();